{
  "name": "rss-to-linkedin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "predeploy": "git add -A && (git diff --staged --quiet || git commit -m 'deploy') && git push",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.82.2"
  }
}
